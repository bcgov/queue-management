name: Parent
on:
  workflow_dispatch:

jobs:
  parent:
    name: Parent
    runs-on: ubuntu-latest

    steps:
    - name: debug
      shell: bash
      run: |
        ls -la /home/runner/work/queue-management/queue-management

    - name: echo
      shell: bash
      run: |
        echo Hello

  child:
    name: child
    uses: walter-moar/queue-management/.github/workflows/child.yaml@v1
