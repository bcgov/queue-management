name: Parent
on:
  workflow_dispatch:

jobs:
  parent:
    name: Parent
    runs-on: ubuntu-latest

    steps:
    - name: child
      uses: ./.github/workflows/child.yaml

    - name: echo
      shell: bash
      run: |
        echo Hello
