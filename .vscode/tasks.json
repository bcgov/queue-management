{
  "tasks": [
    {
      "args": [
        "-c",
        "cd ${workspaceFolder}/api; source .venv/bin/activate; python manage.py db upgrade"
      ],
      "command": "bash",
      "detail": "python manage.py db upgrade",
      "label": "alembic: upgrade - api",
      "group": "build",
      "presentation": {
        "close": true,
        "panel": "new",
        "reveal": "always",
      },
      "type": "shell"
    },
    {
      "args": [
        "-c",
        "cd ${workspaceFolder}/api; uv run gunicorn wsgi --bind=0.0.0.0:5000 --access-logfile=- --config=gunicorn_config.py --reload --timeout=0"
      ],
      "command": "bash",
      "detail": "uv run gunicorn wsgi --bind=0.0.0.0:5000 --access-logfile=- --config=gunicorn_config.py --reload --timeout=0",
      "label": "api: gunicorn hot reload",
      "presentation": {
        "close": false,
        "panel": "new",
        "reveal": "always"
      },
      "type": "shell"
    }
  ],
  "version": "2.0.0"
}
